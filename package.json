{
  "name": "stream",
  "version": "1.0.0",
  "private": true,
  "description": "Share your screen, sound and all, with someone far away — from any device, with nothing to install on their side.",
  "type": "module",
  "bin": {
    "stream": "bin/stream.js"
  },
  "scripts": {
    "start": "node bin/stream.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
